<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:tint="@android:color/white"
        android:title="@string/create"
        android:id="@+id/action_add"
        android:icon="@drawable/ic_add_white"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_changeView"
        android:title="@string/changeView"
        app:showAsAction="never">
        <menu android:id="@+id/view_menu">
            <item android:id="@+id/compact_view"
                android:title="@string/compact_view"/>
            <item android:id="@+id/detailed_view"
                android:title="@string/detailed_view"/>
        </menu>
    </item>

</menu>