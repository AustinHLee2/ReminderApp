<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:tint="@android:color/white"
        android:title="@string/create"
        android:id="@+id/action_add"
        android:icon="@drawable/ic_add_black_24dp"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_sort"
        android:title="@string/sort"
        app:showAsAction="never">
        <menu>
            <item android:id="@+id/sort_by_create"
                android:title="@string/create_date"/>
            <item android:id="@+id/sort_by_due"
                android:title="@string/due_date"/>
            <item android:id="@+id/sort_by_alphabetical"
                android:title="@string/alphabetical"/>
        </menu>

    </item>

    <item android:id="@+id/action_changeView"
        android:title="@string/changeView"
        app:showAsAction="never">
        <menu android:id="@+id/view_menu">
            <item android:id="@+id/compact_view"
                android:title="@string/compact_view"/>
            <item android:id="@+id/detailed_view"
                android:title="@string/detailed_view"/>
        </menu>
    </item>

    <item
        android:id="@+id/action_edit"
        android:title="@string/edit"
        app:showAsAction="never"/>

    <item android:id="@+id/action_options"
        android:title="@string/options"
        app:showAsAction="never"/>

</menu>